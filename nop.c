#include "monty.h"
/**
 * nop - it has no purpose
 * @stack: doesnt matter
 * @line_cnt : for nothing
 * Return: emoty
 */
void nop(stack_t **stack, unsigned int line_cnt)
{
(void) stack;
(void) line_cnt;
}
